npuforge — third-party components and provenance
================================================

The MIT licence in LICENSE covers **this repository's own source**: `native/`,
`tools/`, `app/`, and the documentation. It does not and cannot cover the things
below. Read this before shipping an APK or using any of it commercially.

SCOPE, IN ONE TABLE
-------------------

| Path | Licence |
|---|---|
| `native/`, `tools/`, `app/`, `docs/`, `*.md` | MIT (LICENSE) |
| `template/recipe.bin`, `template/tpl_trim.pack` | ⛔ **non-commercial** — see (2) and `template/README.md` |
| everything the build needs but the repo does not contain | the third party's, see below |

⛔ BLOCKERS FOR REDISTRIBUTION OR COMMERCIAL USE
-----------------------------------------------

1. **Qualcomm AI Runtime (QAIRT / QNN) — redistribution restricted.**
   A working converter needs, on the device: `qnn-context-binary-generator`,
   `libQnnHtp.so`, `libQnnHtpPrepare.so` (79 MB), `libQnnHtpV*.so` / `*Skel.so` /
   `*Stub.so`, `libQnnSystem.so`, `libQnnHtpNetRunExtensions.so`. These are
   Qualcomm's, they are **not** in this repo, and `.gitignore` keeps them out.
   Whether they may be bundled in a shipped app is **UNRESOLVED** and is the
   single thing that decides whether this can be distributed at all. Resolve it
   before writing the app, not after.

2. **The template's provenance is not clean.**
   `template/recipe.bin` and `template/tpl_trim.pack` are derived from a UNet
   graph that was exported with a modified copy of the `diffusers` UNet
   (Apache-2.0 upstream) redistributed by xororz's Local Dream under
   **CC BY-NC 4.0 (non-commercial)**, then converted by QAIRT's tooling. The
   artefacts here contain no xororz source, but the derivation chain exists.
   The template bundle is therefore **non-commercial**, and is marked as such in
   `template/README.md`. Regenerate it from a clean-room export if commercial use
   is ever wanted — `docs/TEMPLATE-AUTHORING.md`. The rest of the pipeline is
   generic over templates, so a clean bundle drops straight in.

3. **`libqnn_model.so` is deliberately absent.**
   The 9.7 MB aarch64 pack-loading library is generated by QAIRT's
   `qnn-model-lib-generator` from converter-emitted C++. Same unresolved question
   as (1). `docs/BUILD.md` says how to regenerate it; it is gitignored.

4. **Earlier SD1.5 conversions downloaded a CLIP/VAE donor from the same NC project.**
   The legacy archive helper in `app/.../Donor.kt` references `DreamShaperV8_qnn2.28_8gen2.zip` from xororz's
   Hugging Face repo and keeps six files from it (395 MB): the text encoder,
   tokenizer and VAE previously borrowed by SD1.5 conversions. These legacy
   archives remain available for backup/restore and retain their original terms.
   New SD1.5 and SDXL conversions read checkpoint-owned weights instead; this
   does not change the separate UNet template provenance in (2).

5. **Checkpoints are the user's problem, and not all are redistributable.**
   Stable Diffusion 1.5 derivatives carry their own terms (CreativeML Open
   RAIL-M and assorted community licences). This tool converts a file the user
   already has. Do not bundle, mirror, or download checkpoints on their behalf
   without checking each one.

Components used, with their own terms
------------------------------------

| What | Where | Licence |
|---|---|---|
| Qualcomm AI Runtime (QAIRT) SDK | not in repo; user supplies | Qualcomm, restricted |
| Android NDK (build only) | not in repo | Apache-2.0 + others |
| Stable Diffusion 1.5 architecture | implied by the template graph | CreativeML Open RAIL-M |
| `diffusers` UNet (via the export path) | not in repo | Apache-2.0 |
| Local Dream (xororz) — export modules used to author the template | **not in repo** | CC BY-NC 4.0 |

What IS original here
---------------------

- `native/tplconv.cpp` — the on-device weight stage: safetensors reader, the
  quantization rules, and the TPLPACK1 writer.
- `tools/tpl_*.py` — template authoring: the numeric tensor mapping, the rule
  discovery and verification, the pack format and its trimmer.
- The measured results in `README.md`.

These are the parts the MIT licence grants.

SDXL components (2026-09-16)
---------------------------
New SD1.5 and SDXL conversions read CLIP and VAE weights from the selected checkpoint.
The native writer and authoring helpers are original source; their MNN 3.6.1
format references are recorded in tools/clip_recipe.py and docs/CLIP-COMPONENTS.md.
Generated MNN graph recipes, the standard tokenizer, QNN libraries and template
packs remain gitignored. They are not relicensed by the repository's MIT licence.
Checkpoint and upstream component terms still apply.

Legacy backup/restore retains support for the user-published shared ZIP from
https://huggingface.co/Mr-J-369/SDXL-OnDevice-Conversion . It contains MNN text
encoders/embeddings/tokenizer and QAIRT 2.50 v75 QNN VAE encoder/decoder. The VAE
source is identified as madebyollin/sdxl-vae-fp16-fix. These components are not
MIT source and are not bundled in this repository. Generated SDXL template
binaries and packs are also excluded from source control. Neither model family
consumes cached shared weights during new conversions.
