# Personal tooling / local launchers -- never part of the project
claude-yolo.bat
*.local.*

# Gradle / IDE
.gradle/
.kotlin/
local.properties
*.iml
.idea/

# ⛔ Local Dream's backend -- built from CC BY-NC 4.0 source. It is used ONLY by
# the debug DSP probe and must never be committed to this MIT repo. See NOTICE.
libstable_diffusion_core.so
canary_*.bin

# Qualcomm runtime -- redistribution restricted, see NOTICE
libQnn*.so
qnn-context-binary-generator
qnn-*
*.apk

# Generated from QAIRT converter output; regenerate per docs/BUILD.md
libqnn_model.so
lib_arm/
lib_tpl/
model_tpl.cpp
model.cpp
model.bin

# Build output (reproducible -- docs/BUILD.md)
tplconv
tplconv_arm
libtplconv.so
libqnnnetrun.so
*.png
*.o
build/

# Big artefacts: checkpoints, packs, context binaries
*.safetensors
*.ckpt
*.pack
!template/tpl_trim.pack
*.bin
!template/recipe.bin
out/
local.properties
